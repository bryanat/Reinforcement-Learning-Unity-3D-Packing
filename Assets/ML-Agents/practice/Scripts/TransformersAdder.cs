using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TransformersAdder : MonoBehaviour
{
    // define fields here =? define components here
    // Rigidbody Component
    public Rigidbody rb;



    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        // var rb = GetComponent<Rigidbody>();
        // Debug.Log("========rb===========");
        // Debug.Log(rb);
    }
}
